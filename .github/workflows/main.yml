# name: main

# on: 
#   schedule:
#     - cron: '*/10 * * * *'

# jobs:
#   job1:
#     name: Debug
#     runs-on: ubuntu-latest

#     steps:
#     - name: Git checkout
#       uses: actions/checkout@v2
#       with: { ref: debug }
#     - name: List files
#       run: ls

# note : Node.js 12 actions are deprecated.
# The process '/usr/bin/git' failed with exit code 1
