name: Call API-updateSecondRoundMovieList

on:
  schedule:
    - cron: '0 */3 * * *'

  # 允許你從 Action 頁簽上手動執行 workflow
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call add and update firstRound movie list route
#         run: echo trigger add and update firstRound movie list
        run: curl -X POST https://project-cheapskate-testing-yongsin0129.vercel.app/api/admin/updateSecondRoundMovieList -d "passwordId=${{secrets.PASSWORDID}}" 
